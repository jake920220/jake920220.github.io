# Writing Guide (준현의 기록)

이 문서는 블로그 글 작성 시 리노가 따라야 하는 규칙이다.

## 공통

- 문체는 기본적으로 **한다/이다** 체로 통일한다. (서평/기술 동일)
- 불필요한 감탄/과장은 줄이고, 결정/근거/트레이드오프를 중심으로 쓴다.
- 내부 링크/파일 경로/스크립트명은 가능한 한 정확히 표기한다.
- 섹션 제목은 가능하면 **어미를 떼고 명사형으로 끝낸다.**
  - 예: "오탐 줄이기: 키워드만으로는 부족하다" → "오탐 줄이기: 키워드만으로는 부족"
  - 예: "운영 관점: 세션 컨텍스트와 크론은 분리한다" → "운영 관점: 세션 컨텍스트와 크론은 분리"
- 기술 포스팅은 섹션 컨텍스트가 바뀌는 지점에서 **H2(##) 전후로 공백을 넉넉히** 두어 문단이 뭉쳐 보이지 않게 한다.
- **마크다운 문법 누출 방지 (볼드/따옴표/백틱 충돌 방지)**:
  - **따옴표와 볼드 중첩 금지**: `**` 볼드 기호 바로 안쪽에 큰따옴표나 쌍따옴표(`"`, `“`, `”`)를 감싸지 않는다. (예: `**"텍스트"**` ❌ → 일반 텍스트로 쓰거나 따옴표 없이 `**텍스트**` ✅). 마크다운 파서가 볼드로 인식하지 못하고 날것의 `**`를 그대로 브라우저에 노출하는 주원인이 된다.
  - **인라인 코드 백틱과 볼드 중첩 금지**: `**`와 백틱(`` ` ``)을 직접 맞닿아 쓰지 않는다. (예: `**`= 수식`원**` ❌ → `- **항목명**: `= 수식`원` ✅).
  - **볼드 남발 지양**: 인용구(`>`), 표(Table) 셀, 본문 서술형 문장 안에서 `**`를 과도하게 남발하지 말고, 불렛 리스트 항목명(`- **항목명**:`) 중심으로 절제하여 사용한다.
  - **난해한 기호 대신 직관적 설명**: 생소한 수식 기호(예: `$O(1)$`, `$O(0)$`)나 불필요한 특수문자 표기 대신 누구나 쉽게 이해할 수 있는 자연스러운 문맥과 비유로 서술한다.
- **기업명 및 민감 정보 마스킹/익명화 규칙**:
  - **본문 일반 텍스트**: 특정 기업명이나 기관명(예: 스파르타, 삼성전자 등)을 직접적으로 언급하지 않고, "실제 기업교육", "국내 주요 대기업", "고객사" 등 완전히 일반화된 표현을 사용한다.
  - **코드 블록 / 디렉토리 / 파일명 예시**: 실제 폴더 구조나 설정 스니펫 예시가 필요한 경우에 한해서만 `삼O전자`, `스O르타`, `G*건설`처럼 한 글자를 가리는 마스킹 형태로 표기한다.
- **문체 다이어트 및 기술적 사실성(Fact-check) 원칙**:
  - AI 생성 글 특유의 반복적인 클리셰 표현(`"개인적으로"`, `"이유는 단순하다"`, `"중요한 점은"`, `"이 차이는 꽤 크다"`, `"~에 가깝다"`)을 남발하지 않고 간결하고 담백하게 서술한다.
  - 단순한 수사적 강조보다 **실제 실행한 코드, 상태 스키마, 테스트 결과, 공식 문서 근거**를 본문에 직접 제시하여 기술적 신뢰도를 높인다.
  - 프레임워크나 외부 툴을 설명할 때 사실과 다른 과장(예: "단 둘뿐인 스택", "모든 규모에서 속도 불변")을 피하고, 실제 아키텍처와 트레이드오프를 정확히 기술한다.
- 초안 자동 작성 시에도 **최종 목적은 검색 노출보다 독자 만족**이다. 억지 키워드 반복보다 맥락, 경험, 판단, 근거를 우선한다.
- 다른 세션/에이전트가 작성하더라도 이 문서를 먼저 보고 맞춘다.

## 메타데이터 / SEO 규칙

- 모든 공개 포스트는 frontmatter에 최소한 아래 항목을 갖추는 것을 기본으로 한다.
  - `title`
  - `meta_title` (권장 길이 55~70자)
  - `description` (1~2문장)
  - `date`
  - `modDatetime` (수정 시 갱신)
  - `categories` (`["tech"]` 또는 `["book-review"]`)
  - `authors` (`["김준현"]`)
  - `tags` (2~4개 제한)
  - `draft`
- `authors`는 개인 블로그의 일관된 정체성을 위해 **`["김준현"]`**으로 통일한다.
- `meta_title`은 가능하면 **검색 결과에서 바로 의미가 보이도록** 작성한다.
  - 길이는 보통 55~70자 안쪽을 선호한다.
  - 핵심 주제 + 맥락 + 필요한 경우 브랜드/도구명을 넣는다.
- `description`은 1~2문장으로 쓰고, **이 글이 무엇을 다루는지 / 왜 읽을 가치가 있는지**를 먼저 말한다.
  - 과장 표현("완벽 가이드", "무조건", "역대급")은 피한다.
  - 검색엔진용 문장처럼만 쓰지 말고 실제 사람이 눌러볼 만한 설명으로 쓴다.
- `excerpt`는 필요한 글에서만 사용하되, 본문 도입과 완전히 같은 문장을 복붙하지 말고 카드/목록에서 잘 읽히는 별도 요약을 우선한다.
- 공개 글은 canonical, sitemap, robots에 반영되는 것을 전제로 작성한다. 얇은 유틸성 글/임시 글은 `draft: true` 상태로 유지한다.

## 태그(Tags) 관리 및 저품질(Thin Content) 방지 규칙

검색엔진(Google)은 글이 1개뿐인 태그 페이지가 대량 생성되면 **저품질(Thin Content) 또는 중복 페이지**로 평가하여 색인에서 제외하거나 사이트 신뢰도를 낮출 수 있다. 따라서 아래 원칙을 반드시 준수한다.

- **포스트당 태그는 2~4개로 제한**: 글의 핵심 주제를 대표하는 키워드만 등록한다.
- **태그 남발 금지**: 일회성 단어, 단순 도서명, 출판사명, 단순 플랫폼명(예: `사락`, `yes24`, `인스타그램` 등)은 태그로 등록하지 말고 본문 키워드로 녹여낸다.
- **기존 태그 재활용 우선**: 새로운 태그를 만들기 전에 기존 태그 목록(`ai`, `자동화`, `openclaw`, `하네스 엔지니어링`, `codex`, `gsd` 등)에서 재사용할 수 있는지 먼저 검토한다.
- **영어 소문자 통일**: 영문 태그는 소문자(`ai`, `openclaw`, `codex` 등)를 기본으로 하여 대소문자로 인한 중복 생성을 방지한다.

## 리다이렉트 및 내부 링크 규칙

- **내부 링크는 항상 최종 URL 사용**: 글 본문이나 메뉴에서 다른 포스트로 링크를 걸 때는 리다이렉트되는 옛 URL이 아니라 최종 Canonical URL을 직접 지정한다. (301 체인을 통한 크롤링 낭비 방지)
- **URL 변경 시 리다이렉트 관리**: slug를 변경할 경우 `astro.config.mjs`의 `redirects`에만 등록하고, `sitemap.xml`에는 절대 등록되지 않도록 한다.

## sitemap / 날짜 규칙

- 현재 블로그는 Astro 빌드 시 `sitemap.xml`을 자동 생성한다.
- 포스트의 `lastmod`는 아래 우선순위로 자동 반영된다.
  1. `modDatetime`
  2. `pubDatetime`
  3. `date`
- 따라서 새 글 작성 시 최소 `date`는 반드시 넣는다.
- 글을 발행 후 실질적으로 수정했다면 `modDatetime`을 갱신하는 방식을 기본으로 한다.
- 발행일과 공개일을 분리하고 싶을 때만 `pubDatetime`을 별도로 사용한다.

예시:

```yaml
title: 예시 글 제목
meta_title: 예시 글 제목 | 검색에서 바로 의미가 보이게
description: 이 글이 무엇을 다루는지와 왜 읽을 가치가 있는지를 1~2문장으로 설명한다.
date: 2026-04-28T00:00:00Z
modDatetime: 2026-04-29T09:30:00Z
categories: ["tech"]
tags: ["openclaw", "자동화", "ai"]
draft: false
```

## 초안 작성 규칙

- 초안 자동 작성 시 아래 순서를 기본으로 한다.
  1. 이 글의 독자가 누구인지 먼저 정한다.
  2. 독자가 얻을 핵심 1~2개를 먼저 정한다.
  3. 제목/메타/도입보다 **본문 구조**를 먼저 잡는다.
  4. 경험, 판단, 시행착오, 근거 링크를 본문에 녹인다.
  5. 마지막에 제목/메타/설명을 다듬는다.
- 초안은 길이보다 **구조와 논지**를 우선한다.
- 특히 기술 글은 “무엇을 했다”보다 아래를 더 우선한다.
  - 왜 그 선택을 했는가
  - 대안은 무엇이었는가
  - 운영 중 어떤 문제가 있었는가
  - 실제로 어떤 기준으로 검증했는가
- 서평은 줄거리 요약보다 아래를 우선한다.
  - 어떤 독자에게 맞는가
  - 인상 깊었던 주장/관점은 무엇인가
  - 내 경험/일/관계와 어떻게 연결되었는가
  - 아쉬운 점이나 한계는 무엇인가

## 콘텐츠 유형별(기술/서평) 포스팅 규칙

### 1. 기술(tech) 포스팅

- 카테고리: `tech`
- 파일 위치: `src/content/posts/<slug>.md`
- 썸네일/이미지 파일 위치 규칙:
  - `public/images/tech/<slug>/...`
  - 예: `public/images/tech/openclaw-review-automation-part1/openclaw-cover.jpg`
- 포스트 frontmatter의 `image` 또는 `thumbnail`은 위 경로를 사용한다.
- 썸네일 카드는 이미지가 카드 영역을 **object-cover**로 채우는 깔끔한 형태를 기본으로 한다.

### 2. 서평(book-review) 포스팅

- 카테고리: `book-review`
- 파일 위치: `src/content/posts/<slug>-book-review.md` (루트 `posts/`에 단일 계층으로 위치)
- 썸네일/이미지 파일 위치 규칙:
  - `public/images/book-review/<slug>/...` 또는 `public/images/og-image.png`
- IT 서적뿐만 아니라 엔지니어링 사고, 리더십, 조직, 인문/생산성 도서 등 폭넓은 시각의 서평을 다룬다.

### 3. 초안(draft) 관리

- 작성 중인 미완성 초안은 `src/content/posts/drafts/` 폴더에 위치시키거나 frontmatter에 `draft: true`를 반드시 설정한다.
- 빌드 파이프라인에서 `draft: true` 및 `drafts/` 하위 파일은 자동으로 프로덕션 배포에서 제외된다.
